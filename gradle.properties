# Gradle arguments (used for setting max memory allocation currently)
org.gradle.jvmargs=-Xmx1G

# Version and packaging info
version=1.0.2
maven_group=xyz.hewkawar
archives_base_name=SSSMP-Installer

# Main class to start with when the jar is run
main_class=xyz.hewkawar.sssmp_installer.NewInstaller
